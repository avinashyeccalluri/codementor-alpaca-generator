const NECK = {
  bendbackward: require('./bend-backward.png').default,
  bendforward: require('./bend-forward.png').default,
  default: require('./default.png').default,
  thick: require('./thick.png').default
}

export default NECK
